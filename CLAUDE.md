# CLAUDE.md - Kweebec Nightmare

A **standalone Hytale horror minigame** for a mod jam, set in a Void-blighted Emerald Grove at perpetual midnight where the peaceful Kweebec tree-folk have been corrupted into something that hunts. It runs fully on its own and carries day-1 *optional* MMO Skill Tree elevation via **outbound native events** (no dependency), and is designed to later fold into [MMO Skill Tree](https://www.curseforge.com/hytale/mods/mmo-skill-tree) as the repeatable dungeon **capstone of the Emerald Wilds / Kweebec questline**.

This is ONE mod shipping **two modes as separate worlds/instances** (no lobby UI). It is a supplemental mod under the **hyMMO monorepo**'s `additional-mods/` (a git submodule; development is launched from hyMMO, like the content packs). **Status: 1.0.0 is the last public release; 1.1.0 / 1.1.1 / 1.1.2 are HELD** (`gradle.properties` `version=1.1.2`) - the public experience is the **Relight & Escape** co-op chase: relight the grove-shrines by OFFERING gathered glow-mushrooms at interactable shrine FURNACES (press F to submit; the furnace lights with green fire when full, via `ShrineSubmitInteraction` + `ChaseMode.lightShrine`), survive the Blighted hunters and the multi-phase **Warden** boss capstone (its MAX health scaled by party size x difficulty at spawn, with a configurable world-map marker that follows it), then HOLD the Heartwood platform together to escape. The **glow-throwable arsenal** (Moonbloom stun / Gustbloom knockback / Mirebloom slow / Emberbloom damage) is gathered and thrown at hunters, the boss, and mobs (per-difficulty damage, asset-driven), and gathering a glow-mushroom **heals** the gatherer (per-difficulty). End-game WIN spoils are **score-tiered loot tables** rolled by run score, with merged reward chips on the results screen, and a persisted **per-difficulty x party-size** LEADERBOARD (the generalized `ziggfreed-common` board: total/best/time sort toggle + a lifetime Stats tab over stunned / moonbloom / shrines). Asset-driven config (pack-authorable codecs + a runtime override API), natural-terrain worldgen, and the `ziggfreed-common` primitive lib it consumes. Survival mode is still reserved.

> **Difficulty / spawn / boss pass (asset-driven, API-overridable; on the `ziggfreed-common` ENCOUNTER framework).** Hunters PUNISH a landed hit: a slow EntityEffect (`KweebecNightmare_HunterSlow_1..4`) + bonus damage, PROXIMITY-STACKING the debuff to a cap (cleared party-wide on a shrine-light), and DESPERATION ENRAGE (no hit in ~20s -> speed/damage burst + howl) - all per-archetype knobs on `HunterArchetypeAsset` with a `RuleSet` baseline, applied in `KweebecDamageSystem` + `AiHunterController`. SPAWN is asset-driven: common's pack-authorable `EncounterRuleAsset` (this mod ships `Server/ZiggfreedCommon/EncounterRules/{CorruptionWave,ShrineReinforce}.json`; the local `hunter/SpawnTrigger` + `hunter/SpawnPlacementKind` enums are the trigger/placement vocabulary), folded `defaults<pack<owner<runtime` (`KweebecNightmareAPI.overrideSpawnRules/scaleSpawnRules`), consuming common's `SpawnRoster`/`EncounterDirector`/`SpawnPlacement`. The multi-phase reskinned **Warden** boss (common's `instance/encounter/MultiPhaseBossAsset`+`MultiPhaseBossConfig`, authored at `Server/ZiggfreedCommon/Bosses/Warden.json` -> `boss/BossController` + `feedback/BossHud` health bar) spawns at gate-open when `RuleSet.bossEnabled()`; its MAX health scales by party size x difficulty at spawn (`ziggfreed-common` `HealthUtil.scaleMaxHealth`) and it drops a configurable world-map marker that follows it. Design + verified seams: [[kweebec-difficulty-spawn-design]]; plan: [[kweebec-difficulty-encounter-framework-pass]]. **The glow-throwable arsenal** ships three Moonbloom-style thrown mushrooms beside Moonbloom (Gustbloom knockback / Mirebloom slow / Emberbloom damage, effects 100% asset-authored - `DamageEffects.Knockback`/`ApplyEffect`/`BaseDamage`), a pack-authorable [`GroveThrowableAsset`](src/main/java/com/ziggfreed/kweebec/asset/CLAUDE.md) grove-distribution type, per-difficulty thrown-mushroom damage (the `ThrowableDamage` preset knob), boss-phase Emberbloom clusters off the common `MultiPhaseBossAsset` throwable knobs, an Emberbloom friendly-fire guard, and the dungeon-crypt chest pool built around them. Id authority [`moonbloom/GlowThrowables`](src/main/java/com/ziggfreed/kweebec/moonbloom/CLAUDE.md); design: [[need-to-author-additional-wobbly-hickey]]. Per-version detail (the 0.5.0 -> 1.0.0 cycle, gather-heal, defeat stinger, merged reward chips) lives in `CHANGELOG.md`.

> **Design + tech are LOCKED and verified.** Read these in the parent repo before building gameplay - they are the authority, this file is the local router:
> - Plan + LOCKED DESIGN: `../../.claude/plans/utilize-a-new-git-snappy-moon.md`
> - Verified call sequences + threading + the in-game test list: `../../.claude/research/kweebec-nightmare-techspec.md` (raw in `../../.claude/research/raw/kweebec-nightmare-techverify*.json`)
> - Original capability mine: `../../.claude/research/kweebec-nightmare-modjam.md`

## The two modes (design target)

Both are 1-4 player co-op with dead-player presence, played in their own per-round instance world. Stakes (rescue style, lives, bleed-out, hunter count/speed, shrine count, round length, corruption ramp) are a **configurable RuleSet** with presets (Amateur / Nightmare / Hardcore) - the replayability pillar; an installed MMO Skill Tree can select/scale them.

- **Chase - "Relight & Escape"** (~9 min, 15-min cap): relight `2 + partySize` grove-shrines (each a hold-in-place bar that pins you and emits noise the hunter hears; corruption rises with time + per shrine, ramping hunter speed + darkness + heartbeat tier); the final shrine opens the Heartwood Gate + a loud alert that locks the nearest survivor. Win = a CO-OP EXTRACTION HOLD: the required survivor group stands on the Heartwood platform TOGETHER for a per-difficulty `extractionHoldSeconds`, resetting if the group breaks (the required set is per-difficulty `extractionMode`: ALL_MOBILE = mobile survivors, EVERYONE = whole party rescue-first); lose = all cocooned or cap. Caught = cocooned in roots, freed by a teammate (revive-in-place). The continuous-hold timer is the reusable `ziggfreed-common` `instance/zone/ZoneHoldTimer` primitive. Build this FIRST.
- **Survival - "Last Light Till Dawn"** (RESERVED / unshipped, ~3.5 min to dawn): defend a heart-sapling + relight wards in a clearing against escalating corrupted-Kweebec lane waves to a pre-dawn surge, played from a **pure top-down locked camera**. Win = reach dawn; lose = heart falls or all down. Downed = teammate-revivable. The top-down read is the demo flex (Phase-0 camera-feel spike is the go/no-go gate; third-person is the fallback).
- **PvP - Kweebec Clash (team brawl) + Kweebec Domination (control point)** - both are IN THE BUILD but **NOT advertised on the public listing** (CHANGELOG 1.0.0 / `CURSEFORGE.md` describes the chase only; they have not been live-playtested). They run on the same instance/round engine over the generic `ziggfreed-common` match resolvers (`instance/match` for Clash, `instance/zone` control state for Domination), seat `teamSize * 2` players into a sealed arena, reskin combatants to `Kweebec_Sapling`, and seed throwable knockback/stun pickup waves. The PvP stakes are flat `RuleSet` knobs (grouped `clash()` / `domination()` views); a PvP host NPC + dialogue picks the mode, team size, and difficulty. Code in [`mode/clash/`](src/main/java/com/ziggfreed/kweebec/mode/clash) + [`mode/domination/`](src/main/java/com/ziggfreed/kweebec/mode/domination).

## Build

Gradle runs via PowerShell (Java 25). Self-contained `build.ps1` builds + installs:
```powershell
cd 'D:\dev\business\hyMMO\additional-mods\kweebec-nightmare'; .\build.ps1
.\build.ps1 -Install:$false     # build only
.\build.ps1 -ModsDir <path>     # explicit install target (else $env:HYTALE_MODS_DIR)
```
Produces `build/libs/KweebecNightmare-<version>.jar` and copies the runtime jar (never `-sources`/`-javadoc`) into the Hytale `Mods/` folder when `HYTALE_MODS_DIR` is set. `.\gradlew.bat build` works too.

**Perfect Utils is a HARD dependency.** `manifest.json` `Dependencies` pins `"narwhals:Perfect Utils": ">=1.0.2 <2.0.0"`, and `build.gradle` adds it `compileOnly files(perfectUtilsJar)` (path in `gradle.properties` `perfectUtilsJar`, the installed jar) so `com.narwhals.perfectutils.api.AggroAPI` compiles. The server provides the jar at runtime; it is the override-only aggro layer used to steer the hunter. Its source is the **Developer-Utils** repo (`D:/dev/business/hytale-clients/Developer-Utils`) - and **Perfect Utils is the MAINTAINER'S OWN mod** ("narwhals" is the maintainer's partner / repo owner; the maintainer is primary dev with merge-to-main), so a server-update build of it is OUR work in that repo, never an external wait.

**`ziggfreed-common` is ALSO a HARD dependency.** A shared, mod-agnostic primitive lib (sibling submodule `additional-mods/ziggfreed-common`, package `com.ziggfreed.common`): 3D sound, camera, asset-index cache, `SurfaceProbe` (floor-snap onto procedural terrain), notifications, HUD helpers. `manifest.json` `Dependencies` pins `"Ziggfreed:ZiggfreedCommon": ">=2.0.0"`; `build.gradle` adds it `compileOnly files(ziggfreedCommonJar)` (path in `gradle.properties`, pointing at `ZiggfreedCommon-2.0.0.jar`). **PARADIGM: a generic, reusable Hytale primitive belongs in `ziggfreed-common` and is consumed here - never reimplemented locally.** Install BOTH dependency jars to `Mods/` (a missing one fails the load / NoClassDefFounds mid-round).

## Layout

```
settings.gradle / gradle.properties / build.gradle    java + api Gradle modules
build.ps1                                              build + auto-install
api/  src/main/java/com/ziggfreed/kweebec/api/         outbound native event types (RoundCompletedEvent, ...) MMO listens to
src/main/resources/manifest.json                       Group:Ziggfreed, ServerVersion ">=0.6.0-pre.13 <0.7.0", IncludesAssetPack:true
src/main/resources/Server/                             the asset pack: HytaleGenerator/ (Biomes/WorldStructures worldgen), Instances/ (per-mode instance.bson),
                                                        Item/ (Block, Items, Interactions, RootInteractions), Entity/{Damage,Effects}, NPC/{Roles,Spawn},
                                                        Models / Prefabs / Drops / Camera / ProjectileConfigs / Languages,
                                                        KweebecNightmare/ (Presets, Hunters, Mutators, GroveThrowables) and
                                                        ZiggfreedCommon/ (Arenas, BandedEffects, Bosses, Dialogues, EncounterRules, Instances, Leaderboard, Lootables, NpcPlacements, Party, PrefabPlacements)
src/main/java/com/ziggfreed/kweebec/         (nearly every package carries a nested CLAUDE.md router - see Architecture)
  KweebecNightmarePlugin.java                          JavaPlugin entry (setup/shutdown), wires the death + damage systems + command + round service + leaderboard
  round/                                               round state machine + RuleSet + instance lifecycle (shared); RuleSet is now SOURCED from asset/ (PresetConfig), not the old enum
  moonbloom/                                           the glowing-mushroom resource id authority (Moonbloom) - gather/cleanse/throw policy lives in arena/, mode/chase/, event/
  score/                                               configurable round scoring (ScoringConfig/ScoreCalculator); the persisted leaderboard is the ziggfreed-common instance/leaderboard primitive, wired via experience/KweebecExperience
  asset/                                               pack-authorable custom asset TYPES (RoundPreset + HunterArchetype + GroveThrowable + Mutator codecs; the spawn-rule + boss codecs are common's) + *Config folds + KweebecAssetRegistrar + KweebecPackControlAsset
  boss/                                                BossController (spawn + party-size x difficulty HP scaling + per-tick HP/phase backstop + world-map marker + despawn) for the multi-phase Warden capstone
  integration/                                         KweebecNightmareAPI - the runtime difficulty-override seam (overridePreset/scaleRuleSet) an installed MMO calls; resolveRuleSet composes defaults<pack<owner<runtime
  mode/chase/                                          the Chase loop (thin leaf over round/); survival mode reserved
  mode/clash/                                          PvP team-brawl mode (in the build, not advertised on the public listing)
  mode/domination/                                     PvP control-point mode (in the build, not advertised on the public listing)
  lobby/                                               KweebecLobby - public-join / launchSolo / party-manager seating into a round (consumed by experience/)
  interaction/                                         registered block interactions (ShrineSubmitInteraction - press-F Moonbloom offering); has a nested CLAUDE.md router
  npc/                                                 LegacyGuideFiles only (retires guide.json / guide-placements.json to .legacy at setup); the Grove Warden is a ziggfreed-common placement asset gated on MMO Skill Tree being ABSENT - has a nested CLAUDE.md router
  hunter/                                              HunterController seam (AI now, human-driven post-jam) + spawn + marked-target
  arena/                                               ArenaLayout anchors + ArenaBuilder prefab stamping
  death/                                               cocoon-on-death system + revive
  atmosphere/                                          frozen dark-midnight + forced weather (policy only; engine delegated to ziggfreed-common world.AtmosphereService / world.ForcedMusicService)
  feedback/                                            custom HUD + 3D-pulse heartbeat + title cards + toasts (engine delegated to ziggfreed-common feedback.Notify / feedback.EventTitles / ui.CustomHudHelper)
  i18n/                                                Lang key registry (the .lang prefix contract)
  command/                                             /kweebec (alias /kn) start|clash|domination|exit|endall|give|score|leaderboard|party|clashhost (the first entry trigger; pad/block/NPC designed-for)
  event/                                               native event POJOs fired on the engine bus + KweebecDamageSystem (throw-stun attribution + damage-taken scoring)
```

## Architecture (per-package routers carry the working detail)

Every domain package under `src/main/java/com/ziggfreed/kweebec/` has a nested `CLAUDE.md` router that loads when you touch that subtree; the parent techspec (linked above) is the deep authority for exact call sequences + file:line. One line each:

- **[`round/`](src/main/java/com/ziggfreed/kweebec/round/CLAUDE.md)** - the round engine: `RoundService` (spawn/resolve/exit authority), `RoundStateMachine` (1 Hz off-thread -> `world.execute`), `InstanceLifecycle` (Path-A spawn/teleport/remove), `RuleSet`/presets, `RoundInstance`/`PlayerRoundState`.
- **[`moonbloom/`](src/main/java/com/ziggfreed/kweebec/moonbloom/CLAUDE.md)** - `Moonbloom` + `GlowThrowables`, the id authority for the glow-mushroom family (charge item / `_Plant` block / the Gust / Mire / Ember siblings).
- **[`score/`](src/main/java/com/ziggfreed/kweebec/score/CLAUDE.md)** - `ScoringConfig` (per-preset, asset-driven weights) + `ScoreCalculator`, the run score every reward tier and leaderboard row is rolled from.
- **[`asset/`](src/main/java/com/ziggfreed/kweebec/asset/CLAUDE.md)** - the pack-authorable custom asset types (RoundPreset / HunterArchetype / GroveThrowable / Mutator) + their `*Config` folds, `KweebecAssetRegistrar` and the `KweebecPackControlAsset` control keys.
- **[`mode/chase/`](src/main/java/com/ziggfreed/kweebec/mode/chase/CLAUDE.md)** - the PREP -> RITUAL -> ESCAPE loop, per-entry Adventure normalization, `ChaseState` corruption.
- **[`mode/clash/`](src/main/java/com/ziggfreed/kweebec/mode/clash)** + **[`mode/domination/`](src/main/java/com/ziggfreed/kweebec/mode/domination)** (no nested router) - the two PvP modes over the shared round engine + `ziggfreed-common` match/zone resolvers. IN THE BUILD but not advertised on the public listing (not live-playtested); describe them as such.
- **[`boss/`](src/main/java/com/ziggfreed/kweebec/boss/BossController.java)** (no nested router) - `BossController`: spawns the multi-phase Warden at gate-open when `RuleSet.bossEnabled()`, scales its MAX health by party size x difficulty, drives the per-tick HP/phase backstop + `BossHud` + world-map marker, despawns on death/teardown.
- **[`lobby/`](src/main/java/com/ziggfreed/kweebec/lobby/KweebecLobby.java)** (no nested router) - `KweebecLobby`: public-join / `launchSolo` / party-manager seating into a round, consumed by `experience/`.
- **[`interaction/`](src/main/java/com/ziggfreed/kweebec/interaction/CLAUDE.md)** - registered block interactions (`ShrineSubmitInteraction`, the press-F Moonbloom offering).
- **[`npc/`](src/main/java/com/ziggfreed/kweebec/npc/CLAUDE.md)** - `LegacyGuideFiles` (the retired `guide.json`/`guide-placements.json` owner files); the Grove Warden itself is the `Kweebec_Grove_Warden` ziggfreed-common placement asset, `Requires`-gated to stand only where MMO Skill Tree is absent.
- **[`hunter/`](src/main/java/com/ziggfreed/kweebec/hunter/CLAUDE.md)** - the `HunterController` seam over the weighted archetype roster: the chase is the role's OWN Hostile AI (no per-tick re-stamp) with override-only `AggroAPI` taunts, plus LIVE corruption-scaled speed (`applySpeed` swaps a pre-authored `KweebecNightmare_HunterPace_*` EntityEffect per hunter as its band changes).
- **[`arena/`](src/main/java/com/ziggfreed/kweebec/arena/CLAUDE.md)** - `ArenaLayout` anchors, the `Default_Flat` floor-Y contract, `ArenaBuilder` prefab stamping.
- **[`death/`](src/main/java/com/ziggfreed/kweebec/death/CLAUDE.md)** - `CocoonOnDeathSystem` (before `PlayerDeathScreen`, `setShowDeathMenu(false)`) + `CocoonService` hold-in-place / `DeathComponent.respawn`.
- **[`feedback/`](src/main/java/com/ziggfreed/kweebec/feedback/CLAUDE.md)** - `RoundFeedback` fan-out, `NightmareHud` (element-id contract vs the `.ui`), 3D heartbeat.
- **[`atmosphere/`](src/main/java/com/ziggfreed/kweebec/atmosphere/CLAUDE.md)** - frozen midnight + validated forced weather.
- **[`i18n/`](src/main/java/com/ziggfreed/kweebec/i18n/CLAUDE.md)** - `Lang` key registry + the `.lang` filename-prefix contract.
- **camera (reserved survival mode)** - the top-down `SetServerCamera` apply/reset now lives ONLY in ziggfreed-common (`com.ziggfreed.common.camera.ServerCameraService`); the dead byte-identical kweebec copy was removed. Consume common's when survival mode lands.
- **[`event/`](src/main/java/com/ziggfreed/kweebec/event/CLAUDE.md)** - `RoundEvents` outbound native `IEvent<Void>` POJOs (the entire MMO integration surface; see below).
- **[`command/`](src/main/java/com/ziggfreed/kweebec/command/CLAUDE.md)** - `/kweebec` (alias `/kn`) `start|clash|domination|exit|endall|give|score|leaderboard|party|clashhost [preset]` (a direct entry trigger).
- **`experience/` + `dialogue/`** (consumer wiring over the ziggfreed-common instance layer; no nested router) - the entry/matchmaking UX. The guide NPC dialogue's difficulty options now OPEN the reusable **Play screen** (`common.instance.play.PlayModePage`: a Public / Party / Solo chooser that morphs to a live roster whose launch timer **ticks down on screen**) via `dialogue/OpenPlayAction` (the `{ "Play": "<preset>" }` sugar) - they do NOT queue immediately (the old `StartRoundAction` is gone). `experience/KweebecPlayMode` maps the three cards to `KweebecLobby` (public join / `launchSolo` / open the party manager carrying the chosen difficulty); `KweebecExperience` builds the `PlayMode`/`Party`/`Results`/`Leaderboard` page deps. The mode cards + queue policy are **asset-driven** in `Server/ZiggfreedCommon/Instances/*.json` (`QueueModes`), never Java-baked. **End-game WIN rewards are score-tiered lootables**: `KweebecExperience.stashResults` DECIDES the chase preset's `RewardTableId` (`Server/ZiggfreedCommon/Lootables/Chase_*.json`, the common `Lootable` primitive - a guaranteed `Rolls` payout plus a `Pool` whose pick count climbs with the score) through `LootEngine.select` against `LootFactors.lookupFor(score, win)`, converts the answer with `DeferredRewards`, and persists that concrete list to the durable claim store, stashing the same list for the results-page chips (delivered on the overworld Claim, full-inventory guarded). Another mod enriches those tables by shipping a lootable with `ContributesTo`, without touching kweebec's files. Win-only; the in-arena crypt chest stays native. See [[kweebec-playmode-public-party-solo]]; the dialogue, leaderboard and party configs are pack assets in the shared folders too (`Server/ZiggfreedCommon/{Dialogues,Leaderboard,Party}/`, resolved through the shared dialogue store and `LeaderboardLayoutConfig` / `PartySettingsConfig`, with the Java `PartyConfig` left only as the unauthored fallback), per [[ziggfreed-common-asset-driven-configs]].

**Instance + reskin authoring (asset JSON, no router):** each mode is a pack instance `Server/Instances/<mode>/instance.bson` shipped as plain JSON (the engine LOADS JSON via `WorldConfig.load` -> `RawJsonReader.readSync`; BSON is only what `/instances edit save` writes, never a load requirement). It MUST author `RemovalConditions` (`WorldEmpty`/timeout) + `DeleteOnRemove=true` or worlds leak, and `SpawnPoint.Y` MUST match the worldgen floor (see [`arena/`](src/main/java/com/ziggfreed/kweebec/arena/CLAUDE.md)). The hunter role clones `Template_Kweebec_Razorleaf` (`DefaultPlayerAttitude:Hostile`, drop the Target sensor's Attitude filter + Range). Reskin = extend `Kweebec_Sapling` (`Parent` + dark texture + Void-glow `ModelParticle[]` on bones `Chest`/`L-Eye-Attachment`/`R-Eye-Attachment`), no new mesh.

**Threading (load-bearing, every package):** off-thread scheduler -> `world.execute` hop -> all Store/Ref/Sound/HUD/camera/weather on the world thread; packet writes are thread-safe; `spawnInstance`/`removeWorld` never `.join()` on a world tick thread.

## MMO Skill Tree integration (optional, outbound events - NO dependency)

The mod has **zero compile/runtime dependency on MMO Skill Tree**. It fires its own native events on the shared engine bus: `HytaleServer.get().getEventBus().dispatchFor(E.class); if (d.hasListener()) d.dispatch(new E(...))` (sync `IEvent<Void>` POJOs in `api/`). MMO consumes them MMO-side via the reflective `registerGlobal` adapter (the `AnglersAlmanacAdapter` pattern: load the event class from THIS mod's classloader so `Class` identity matches, then `getEventRegistry().registerGlobal`). When MMO is absent, the mod self-contains its own rewards. MMO can also reflectively call back into a small `KweebecNightmareAPI` to scale difficulty / replayability. Fire from a world-thread context so listeners can hop to `world.execute`.

## Release notes (patch-notes paradigm)

Per-version public release notes in `patch-notes/<version>.md`, same paradigm as the parent mod repo: frontmatter (`version`, `title`, `type: patch-note`, `status: held|released`), a one-line summary, then `- **New/Fixed: ...**` bullets; `patch-notes/_INDEX.md` newest-first. `CHANGELOG.md` is the dev changelog, `CURSEFORGE.md` the public listing, `README.md` the GitHub front page. **No em-dashes.** **Describe shipped reality, not aspiration** - at scaffold stage say "scaffold", not "adds a horror minigame".

## Conventions

PascalCase asset filenames; codec JSON keys start upper-case. `@Nonnull`/`@Nullable` on params; log through `util.SafeLog` on any parse / validate / asset-load path (a static facade over ziggfreed-common's `GuardedLogger` - the raw flogger throws in a log-manager-less unit JVM), and `KweebecNightmarePlugin.LOGGER` directly in ordinary world-thread runtime code. Localize all player-facing text via `Message`/lang keys from day 1 (no raw display strings); en-US complete. Package root `com.ziggfreed.kweebec`. As a submodule, the order is fixed: commit + push HERE first, verify the SHA is on the remote, THEN bump the gitlink in the parent hyMMO repo.

## Gotchas (each was a shipped 0.2.0 bug; the full detail lives in the package router)

- **`.lang` keys are unprefixed - the FILENAME is the namespace.** Prefixing them doubles the key and shows raw keys on screen. See [`i18n/`](src/main/java/com/ziggfreed/kweebec/i18n/CLAUDE.md).
- **Spawn/arena Y must match the worldgen floor** (`Default_Flat` `Base`=80 -> Y=80; the old Y=65 buried players in terrain). See [`arena/`](src/main/java/com/ziggfreed/kweebec/arena/CLAUDE.md).
- **Survivor game mode is set to Adventure ONCE on entry, never per-tick** (per-tick forcing fought a Creative admin). See [`mode/chase/`](src/main/java/com/ziggfreed/kweebec/mode/chase/CLAUDE.md) + [`hunter/`](src/main/java/com/ziggfreed/kweebec/hunter/CLAUDE.md).
- **Perfect Utils (`narwhals:Perfect Utils`) is a HARD manifest dependency** - its `AggroAPI` is the override-only hunter aggro layer; missing the jar at runtime fails the load. Compile path lives in `gradle.properties` (`perfectUtilsJar`); source is the Developer-Utils repo (see Build).
- **A reskinned/cloned NPC role TEMPLATE must HANDLE every state it sets or exports, or the whole `Abstract` builder fails validation - and every `Variant` that `References` it then fails as `unknown builder`, so the entity silently NEVER spawns** (no exception at the spawn call site; the role just no-shows). The 0.5.0 Warden boss shipped this: cloned from the Blight but DROPPED the `Alerted` state handler while its `Component_Kweebec_Instruction_Search` still listed `Alerted` in `_ExportStates`, so the role failed validation (`State setter ... without accompanying state/setter: Alerted`), `BossController.spawn` returned false, and the escape gate opened with NO boss (the no-soft-lock fallback). Fix = a `{Sensor:{Type:State,State:X}}` handler for every set/exported state (the Warden's `Alerted` escalates straight to `Attack`; clone the FULL state machine from the proven base role, do not trim handlers). The SERVER LOAD LOG names the offending state + role (`[NPC|P] FAIL: <template>.json: ...`) - read it FIRST when a custom NPC will not spawn. Unknown top-level keys also bite: the old `UseCombatActionEvaluator`/`_CombatConfig` CAE pair REJECTS the builder; a stray `$Comment_*` (vs the tolerated `$Comment`) only logs `Unknown JSON attribute`.
- **Dialogue types must be registered in plugin `setup()`, never lazily on first use.** `ziggfreed-common`'s dialogue store is Pattern A: every conversation file decodes ONCE at `LoadAssetEvent`, right after every plugin's `setup()` returns, against the shared decode vocabulary every mod registers into. `KweebecDialogue.init()` (contributes `Play`/`NotInRound`/`Engaged` and declares this mod's key namespace) is called explicitly from `KweebecNightmarePlugin.setup()` for exactly this reason (1.1.2 fix) - registering lazily on first NPC interaction, as it did before, misses the read and every dialogue file fails to load that boot. There is no lazy fallback left to lean on - `init()` is the only registration path, and a late registration would still populate the table but every dialogue file naming a late type has already failed to load by then.
- **Kweebec does NOT own a dialogue engine; there is ONE per server and this mod contributes to it.** `KweebecDialogue.init()` calls `DialogueEngine.registerShared(...)` for each of its three types and holds no engine handle of its own, so on a server also running another dialogue mod each mod's actions and conditions actually execute in whichever conversation they appear in, instead of only in the ones its own screens opened. There is nothing to register for the SCREEN: the conversation page is the library's, built from process-wide state, so a Kweebec conversation and an MMO one read the same whichever character opened them. All this mod declares is its own namespace (`ContentKeys.install(new I18nModuleContentI18n("kweebecnightmare."))`), which is how its authored keys resolve wherever they are painted; its two conversations carry a `Chrome` block so the Grove Warden keeps saying "Step away" rather than the library's "Farewell".
